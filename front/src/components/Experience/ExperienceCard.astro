---
import type { Experience } from "../../types/types";
import { getFormatedPeriode } from "../../utils/date";

interface Props {
  experience: Experience
}

const { experience } = Astro.props as Props;

const periode = getFormatedPeriode(experience.startPeriode, experience.endPeriode);


---

<div class="flex">
  <div class="flex-1">
    <p>{experience.enterprise}</p>
    <p>{periode}</p>
    <p>{experience.city}</p>
    <div class="">
      <img src={experience.image} alt={experience.enterprise}>
    </div>
  </div>

  <div class="flex-1">
    <h3>{experience.title}</h3>
    <ul>
      {experience.tasks.map(task => ( 
        <li>{task}</li>
      ))}
      <li></li>
    </ul>
  </div>
</div>